{
	"homepage": "https://jos-cabrera.github.io/Pomodoro/",
	"name": "pomodoro",
	"private": true,
	"version": "0.0.0",
	"type": "module",
	"scripts": {
		"dev": "vite",
		"predeploy": "npm run build:web",
		"deploy": "gh-pages -d dist",
		"build": "BUILD_TARGET=electron vite build",
		"build:web": "vite build",
		"build:electron": "npm run generate-icons && BUILD_TARGET=electron vite build && electron-builder",
		"dev:electron": "concurrently \"npm run dev\" \"wait-on http://localhost:5173 && NODE_ENV=development electron electron/main.cjs\"",
		"start:electron": "npm run build && electron electron/main.cjs",
		"dist": "npm run generate-icons && npm run build && electron-builder --publish=never",
		"dist:all": "npm run generate-icons && npm run build && electron-builder --publish=never --win --mac --linux",
		"dist:appimage": "npm run generate-icons && npm run build && electron-builder --linux AppImage --publish=never",
		"generate-icons": "node scripts/generate-icons.cjs",
		"lint": "eslint . --report-unused-disable-directives --max-warnings 0",
		"preview": "vite preview"
	},
	"dependencies": {
		"react": "^18.3.1",
		"react-dom": "^18.3.1"
	},
	"devDependencies": {
		"@eslint/js": "^9.17.0",
		"@types/react": "^18.3.12",
		"@types/react-dom": "^18.3.1",
		"@vitejs/plugin-react": "^4.3.3",
		"concurrently": "^9.2.1",
		"electron": "^39.2.7",
		"electron-builder": "^26.0.12",
		"eslint": "^9.17.0",
		"eslint-plugin-react": "^7.37.2",
		"eslint-plugin-react-hooks": "^5.0.0",
		"eslint-plugin-react-refresh": "^0.4.14",
		"gh-pages": "^6.2.0",
		"globals": "^15.12.0",
		"sharp": "^0.33.5",
		"vite": "^6.0.5",
		"wait-on": "^9.0.3"
	},
	"description": "This is a pomodoro timer made in react. Style was influenced by [pomofocus](https://pomofocus.io/).\r More features might be added in future versions.\r All times in settings must be in minutes.",
	"main": "electron/main.cjs",
	"keywords": [],
	"author": {
		"name": "Pomodoro Timer",
		"email": "pomodoro@example.com"
	},
	"license": "ISC",
	"build": {
		"appId": "com.pomodoro.timer",
		"productName": "Pomodoro Timer",
		"directories": {
			"output": "release"
		},
		"files": [
			"dist/**/*",
			"electron/**/*",
			"node_modules/**/*"
		],
		"mac": {
			"category": "public.app-category.productivity",
			"target": [
				{
					"target": "dmg",
					"arch": [
						"x64",
						"arm64"
					]
				},
				{
					"target": "zip",
					"arch": [
						"x64",
						"arm64"
					]
				}
			],
			"icon": "icon.svg"
		},
		"win": {
			"target": [
				{
					"target": "nsis",
					"arch": [
						"x64",
						"ia32"
					]
				},
				{
					"target": "portable",
					"arch": [
						"x64",
						"ia32"
					]
				}
			],
			"icon": "icon.svg"
		},
		"linux": {
			"target": [
				{
					"target": "AppImage",
					"arch": [
						"x64"
					]
				},
				{
					"target": "deb",
					"arch": [
						"x64"
					]
				},
				{
					"target": "rpm",
					"arch": [
						"x64"
					]
				}
			],
			"icon": "build/icons/512x512.png",
			"category": "Office",
			"synopsis": "A productivity-focused Pomodoro timer",
			"description": "A productivity-focused Pomodoro timer with desktop notifications and focus session management."
		},
		"nsis": {
			"oneClick": false,
			"allowToChangeInstallationDirectory": true,
			"createDesktopShortcut": true,
			"createStartMenuShortcut": true
		}
	}
}
